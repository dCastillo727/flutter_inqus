name: flutter_inqus
description: "A new Flutter package project."
version: 0.0.1
homepage: "https://github.com/dCastillo727/flutter_inqus"
repository: "https://github.com/dCastillo727/flutter_inqus"

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.3.0
  meta: ^1.16.0
  provider: ^6.1.4

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
